- if @resource
  - case @resource.class.name
  - when 'User', 'Business'
    - path_array = [@resource, @calendar]
    - url = resource_calendars_path(@resource)
  - when 'Household'
    - path_array = [@resource, @calendar]
    - url = user_home_calendars_path(@user)
  - when 'Group'
    - path_array = [@resource, @calendar]
    - url = group_calendars_path(@resource)
- else
  - path_array = @calendar
  - url = calendars_path

.grid-container.small-12.medium-10

  .grid-content.small-12
    %h3 New calendar
    = form_for path_array, :url => url do |f|
      %section.block-list
        %header Calendar
        %ul
          %li
            = f.text_field :name, {:placeholder => 'Name'}
          %li
            = f.text_area :description, {:placeholder => 'Description'}

      %button.button.expanded.bg-blueberry Save
